spring.application.name=ManjeetSinghPortfolio

# Server configuration
# Remove fixed port so Render can inject $PORT automatically
# Context path set to root for clean public URL
server.servlet.context-path=/

# Disable Thymeleaf cache in dev; set to true for production performance
spring.thymeleaf.cache=true
